experiment: cartpole_mlp_pilot
env_id: CartPole-v1
agent: dqn_mlp
seeds: [0]
episodes: 25
gamma: 0.99
learning_rate: 0.001
batch_size: 16
learning_starts: 16
buffer_size: 50000
target_update_interval: 250
epsilon_start: 1.0
epsilon_end: 0.05
epsilon_decay_steps: 5000
gradient_clip_norm: 10.0
model:
  hidden_sizes: [6]
outputs_dir: outputs

